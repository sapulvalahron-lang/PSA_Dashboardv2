/**
 * System-wide constants.
 * Keeps hardcoded strings out of the logic.
 */
const SYSTEM_CONSTANTS = {
  SHEETS: {
    CONFIG: "_Config",
    DASHBOARD_DATA: "Dashboard_Data",
    METADATA: "Dashboard_Metadata",
    ETL_LOG: "_ETL_Log"
  }
};